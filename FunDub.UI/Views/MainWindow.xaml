<Window x:Class="FunDub.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FunDub.UI.Views"
        mc:Ignorable="d"                   
        Title="Fun Dub Tool Box - Refreshed" Background="#1E1E1E" Foreground="White"
        Height="720" Width="1280"
        WindowStartupLocation="CenterScreen">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Grid.Row="0" Background="#252526" BorderBrush="#333" BorderThickness="0,0,1,0">
            <Grid Margin="10">
                <TextBlock Text="Materials" DockPanel.Dock="Top" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" VerticalAlignment="Top"/>

                <StackPanel Margin="0,35,0,0">
                    <ToggleButton x:Name="IntroTB" Style="{StaticResource SidebarButtonStyle}" Command="{Binding SelectIntroCommand}" Click="IntroTB_Click">
                        <Grid HorizontalAlignment="Stretch" Width="180">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="&#xe892;"  FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="INTRO" VerticalAlignment="Center"/>
                            </StackPanel>

                            <ToggleButton x:Name="IntroSaveTB" HorizontalAlignment="Right" Background="#3F3F3F" Padding="5" VerticalAlignment="Center" Click="IntroSaveTB_Click">
                                <TextBlock Text="💾" FontSize="18" Foreground="{StaticResource TextMuted}"/>
                            </ToggleButton>
                        </Grid>
                    </ToggleButton>
                    <ToggleButton x:Name="VideoTB" Style="{StaticResource SidebarButtonStyle}" Margin="0,0,0,10" Click="VideoTB_Click">
                        <Grid HorizontalAlignment="Stretch" Width="180">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="&#xe8b2;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="VIDEO" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </ToggleButton>
                    <ToggleButton x:Name="LogoTB" Style="{StaticResource SidebarButtonStyle}" Click="LogoTB_Click">
                        <Grid HorizontalAlignment="Stretch" Width="180">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="&#xeb9f;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="LOGO" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ToggleButton x:Name="LogoSaveTB" HorizontalAlignment="Right" Background="#3F3F3F" Padding="5" VerticalAlignment="Center" Click="LogoSaveTB_Click">
                                <TextBlock Text="💾" FontSize="18" Foreground="{StaticResource TextMuted}"/>
                            </ToggleButton>
                        </Grid>
                    </ToggleButton>
                    <ToggleButton x:Name="SubtitlesTB" Style="{StaticResource SidebarButtonStyle}" Click="SubtitlesTB_Click" >
                        <Grid HorizontalAlignment="Stretch" Width="180">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="&#xe7f0;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="SUBTITLES" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </ToggleButton>
                    <ToggleButton x:Name="AudioTB" Style="{StaticResource SidebarButtonStyle}" Click="AudioTB_Click" >
                        <Grid HorizontalAlignment="Stretch" Width="180">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="&#xe995;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="AUDIO" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </ToggleButton>
                    <ToggleButton x:Name="OutroTB" Style="{StaticResource SidebarButtonStyle}" Click="OutroTB_Click" >
                        <Grid HorizontalAlignment="Stretch" Width="180">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="&#xe893;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="OUTRO" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ToggleButton x:Name="OutroSaveTB" HorizontalAlignment="Right"  Background="#3F3F3F" Padding="5" VerticalAlignment="Center" Click="OutroSaveTB_Click">
                                <TextBlock Text="💾" FontSize="18" Foreground="{StaticResource TextMuted}"/>
                            </ToggleButton>
                        </Grid>
                    </ToggleButton>
                </StackPanel>

                <ToggleButton DockPanel.Dock="Bottom" Style="{StaticResource SidebarButtonStyle}" VerticalAlignment="Bottom" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                    <Grid HorizontalAlignment="Stretch" Width="180">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBlock Text="&#xec7a;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="TOOLS" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </ToggleButton>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="15,10,15,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="1.5*"/>            
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid>
                    <TextBlock Text="Materials List" DockPanel.Dock="Top" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top"/>

                    <ListView x:Name="MaterialsLB" ItemsSource="{Binding Materials}" BorderBrush="#3F3F3F" BorderThickness="1,0,1,1"  d:ItemsSource="{d:SampleData ItemCount=5}"  Background="Transparent" Margin="5,30,5,5" AllowDrop="True" DragEnter="MaterialsLB_DragEnter" DragLeave="MaterialsLB_DragLeave" Drop="MaterialsLB_Drop" FontSize="20">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Type" Width="80">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Type}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Header="Path" Width="Auto">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Path}" Foreground="#AAAAAA" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Header="Delete" Width="Auto">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content=""  FontFamily="Segoe MDL2 Assets"  Background="Transparent" 
                                                Foreground="White" BorderThickness="0"
                                                Command="{Binding DataContext.RemoveMaterialCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Grid>

           

            <Grid Grid.Row="2" Grid.Column="1" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Project Settings" DockPanel.Dock="Top" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top"/>

                <UniformGrid Grid.Row="1" Columns="3" Rows="1">
                    <GroupBox Header="Export Quality" Style="{StaticResource SettingsGroupBoxStyle}" >
                        <UniformGrid Columns="4" Margin="0,5">
                            <RadioButton Content="Low" Style="{StaticResource QualityRadioButtonStyle}" x:Name="QualityLow"/>
                            <RadioButton Content="Medium" Style="{StaticResource QualityRadioButtonStyle}" x:Name="QualityMed" />
                            <RadioButton Content="High" Style="{StaticResource QualityRadioButtonStyle}" x:Name="QualityHigh"/>
                            <RadioButton Content="Source" Style="{StaticResource QualityRadioButtonStyle}" x:Name="QualitySource" IsChecked="True"/>
                        </UniformGrid>
                    </GroupBox>

                    <GroupBox x:Name="LogoSettingsGB" Visibility="Hidden" Header="Logo" Style="{StaticResource SettingsGroupBoxStyle}" Margin="10,0,10,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <UniformGrid Grid.Column="0" Columns="2" Width="80">
                                <RadioButton Content="↖" FontSize="10" Margin="1" GroupName="LogoPosition" Style="{StaticResource QualityRadioButtonStyle}"/>
                                <RadioButton Content="↗" FontSize="10" Margin="1" GroupName="LogoPosition" Style="{StaticResource QualityRadioButtonStyle}" IsChecked="True"/>
                                <RadioButton Content="↙" FontSize="10" Margin="1" GroupName="LogoPosition" Style="{StaticResource QualityRadioButtonStyle}"/>
                                <RadioButton Content="↘" FontSize="10" Margin="1" GroupName="LogoPosition" Style="{StaticResource QualityRadioButtonStyle}"/>
                            </UniformGrid>

                            <Grid Grid.Column="1" Margin="10,0">
                                <TextBlock Text="Transparency" Style="{StaticResource SettingLabelStyle}" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Top"/>
                                <TextBox Text="100%" TextAlignment="Center" Height="25" VerticalContentAlignment="Center"/>
                            </Grid>

                            <RadioButton Grid.Column="2" GroupName="LogoPosition" Style="{StaticResource QualityRadioButtonStyle}" Content="Set Manual" VerticalAlignment="Center" Height="25" Padding="5,0" HorizontalAlignment="Center" Margin="0,0,0,0"/>
                        </Grid>
                    </GroupBox>

                    <GroupBox Header="Additional" Style="{StaticResource SettingsGroupBoxStyle}">
                        <StackPanel VerticalAlignment="Center">
                            <CheckBox Content="Export To Source Folder" Foreground="White" Margin="0,2"/>
                            <CheckBox Content="Open Folder After Export" Foreground="White" Margin="0,2"/>
                            <Button Content="More Settings" HorizontalAlignment="Right" Margin="0,5,0,0"/>
                        </StackPanel>
                    </GroupBox>
                </UniformGrid>
                
                <StackPanel Grid.Row="2" Grid.Column="1">
                    <TextBlock Text="Processing" DockPanel.Dock="Top" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"/>
                    <Border   Background="#2D2D2D" Padding="10">
                        <UniformGrid Rows="1" Columns="3">

                            <Button Background="#333333" Foreground="White" HorizontalAlignment="Left" Padding="10">
                                <Grid Width="Auto">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xe710;" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                    <TextBlock x:Name="AddToQueueTextBlock" Text="Add Project to Queue" FontSize="14" HorizontalAlignment="Center" Margin="20,0,0,0" TextAlignment="Center"/>
                                </Grid>
                            </Button>
                            
                            <Label x:Name="ItemsInQueueLabel" Content="Items in queue: 0" FontWeight="Bold" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            
                            <Button Background="#333333" Padding="10" Foreground="White" HorizontalAlignment="Right"  HorizontalContentAlignment="Center">
                                <Grid>
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8FD;" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                    <TextBlock x:Name="SeeQueueListTextBlock" Text="See Queue List" FontSize="14" HorizontalAlignment="Center" Margin="20,0,0,0" TextAlignment="Center"/>
                                </Grid>
                            </Button>
                        </UniformGrid>
                    </Border>
                </StackPanel>
               

            </Grid>
        </Grid>

        <Border Grid.Row="1" Grid.Column="0" Background="#2D2D2D" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="🗑" Width="40" Margin="0,0,5,0"  Background="#333333" Foreground="White" FontSize="26"/>
                <Button Content="⚙" Width="40" Margin="0,0,5,0" Background="#333333" Foreground="White" FontSize="26"/>
                <Button Content="&#xe946;" FontFamily="Segoe MDL2 Assets"  Width="40" Background="#333333" Foreground="White" FontSize="30" Padding="1,4,1,0" />
            </StackPanel>
        </Border>

        <Button x:Name="FirstStratButton" Grid.Column="1" Grid.Row="1"  Background="#FF107C10" Width="200" Foreground="White" HorizontalAlignment="Center" BorderBrush="{x:Null}" Margin="0,0,10,0" VerticalAlignment="Center" HorizontalContentAlignment="Center" Padding="0,0,0,0" Click="FirstStratButton_Click">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xEDB5;" FontSize="25" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <TextBlock x:Name="StartTextBlock"  Text="Start" FontSize="28" HorizontalAlignment="Center" Margin="30,0,0,0" TextAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Button>
        
        
        
        
    </Grid>
</Window>
